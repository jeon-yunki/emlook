# Configure Account Service
eureka:
  instance:
    hostname: emlook-account-service
    nonSecurePort=80
    
     
server:
  port: 18091   # HTTP (Tomcat) port

# Discovery Server Dashboard uses FreeMarker.  Don't want Thymeleaf templates
#  thymeleaf:
#    enabled: false     # Disable Thymeleaf 
